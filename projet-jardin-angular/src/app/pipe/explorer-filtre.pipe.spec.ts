import { ExplorerFiltrePipe } from './explorer-filtre.pipe';

describe('ExplorerFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new ExplorerFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
